import { YelloeFellowDirective } from './yelloe-fellow.directive';

describe('YelloeFellowDirective', () => {
  it('should create an instance', () => {
    const directive = new YelloeFellowDirective();
    expect(directive).toBeTruthy();
  });
});
